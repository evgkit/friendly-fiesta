<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.evgkit.simpleandroidapp">

    <uses-permission android:name="android.permission.INTERNET" />

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/AppTheme">
        <activity android:name=".ui.MainActivity">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity android:name=".ui.AnotherActivity" />
        <activity android:name=".ui.CatsActivity" />
        <activity android:name=".ui.LayoutTestActivity" />
        <activity android:name=".ui.threads.ThreadsExerciseActivity" />
        <activity android:name=".ui.threads.primitiveAsyncTask.AsyncTaskActivity" />
        <activity android:name=".ui.threads.threadsAsyncTask.ThreadsActivity" />
    </application>

</manifest>